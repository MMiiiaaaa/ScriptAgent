# 复制为 .env 后填写（且勿将 .env 提交到 Git）
# 公网多用户时：可只配 SCRIPT_AGENT_MODE=real，不在此放 OPENAI_API_KEY，由网页用户自备 Key

SCRIPT_AGENT_MODE=real
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# OPENAI_MODEL=gpt-4o-mini

# 可选：限制谁可调用 /api/*
# SCRIPT_AGENT_API_KEY=
